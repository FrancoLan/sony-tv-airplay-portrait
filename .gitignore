# Build trees and caches — everything here is regenerated by build.sh / gradlew
build/
.gradle/
.cxx/
captures/
.externalNativeBuild/

# Machine-specific: SDK path and signing config
local.properties

# Upstream clone lives outside the project (NDK cannot handle spaces in paths)
airplay-portrait/src

# Prebuilt binaries: 25 MB, and GPL-3.0 binaries should not be distributed
# without their corresponding source. build.sh reproduces them from the patch.
apks/
*.apk

.DS_Store
.idea/
*.iml
